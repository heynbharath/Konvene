NEXT_PUBLIC_API_URL=http://localhost:4000

# Public/anon values only — safe to expose to the browser. Get these from
# Supabase Dashboard -> Project Settings -> API Keys. Required for Google/
# GitHub OAuth login, which has to run client-side.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
